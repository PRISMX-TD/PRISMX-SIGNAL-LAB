# 这些文件要在 Linux 上被解析/执行，无论在哪个系统提交都必须保持 LF。
# CRLF 会让 VPS 上的 bash 报 `$'\r': command not found`，
# 也会让 GitHub Actions 里 run: 块的多行脚本出同样的错。
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
# Python 源码一律按文本处理，行尾在库里归一为 LF。
# 显式声明是必需的，不能依赖 git 的自动判定：它的二进制启发式会把
# "非 ASCII 字节数 > 可打印字节数/128" 的文件判成 binary，而本仓库的注释是
# 中英双语、中文密度高——routers/gateway.py 就恰好越过了那条线，长期被当成
# 二进制文件：不做行尾归一（库里存的是 CRLF，其余 .py 都是 LF）、diff 不可读、
# blame 与合并也都失效。加一行英文注释就可能让判定翻回来，于是同一个文件时而
# 文本时而二进制，改动量看着毫无道理。
# Force text handling for Python sources. Git's binary heuristic flags files
# whose non-ASCII byte count exceeds printable/128 — dense Chinese comments push
# routers/gateway.py over that line, so it was stored unnormalised and diffed as
# binary. Explicit attribute, not auto-detection.
*.py text
# 同一条启发式也会咬到网关的 C# 源码与 EA 的 MQL5 源码：Mt5Link.cs 150 KB 中英双语
# 注释，密度不输 routers/gateway.py。桥接的 PyInstaller spec 内容是 Python 但扩展名
# 不在 *.py 之内，一并声明。
# The same heuristic bites the gateway's C# and the EA's MQL5 sources (Mt5Link.cs is
# 150 KB of bilingual comments), and the bridge's PyInstaller spec is Python under a
# non-.py extension.
*.cs text
*.mq5 text
*.mqh text
bridge/PRISMX-Bridge.spec text
# ops/ 下是原样拷到 Linux 服务器上的配置（nginx、systemd 单元、脚本）。Windows 上检出成
# CRLF 再拷过去，systemd 与 nginx 会把 \r 当成值的一部分。
# Server configs under ops/ are copied verbatim to Linux hosts; keep them LF.
ops/** text eol=lf
