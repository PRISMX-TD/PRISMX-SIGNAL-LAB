# PRISMX Signal Lab - 后端依赖
#
# 安全更新 2026-09-19：fastapi / starlette / python-multipart / requests 升到修复版本。
# 幅度刻意控制在 fastapi 的 0.115 补丁线内（0.115.0 → 0.115.14），不跳 0.116+：
# 部署流程是服务器上 git pull + pip install + 重启，装包那步出问题会直接让一次部署
# 静默停在旧代码上（见运维手册），破坏性变更的代价远高于收益。
# Security refresh 2026-09-19. Kept inside fastapi's 0.115 patch line on purpose:
# the deploy flow is git pull + pip install + restart on the server, and a problem
# at the install step silently leaves a deploy on the old code.
fastapi==0.115.14           # 0.115.0 带的 starlette<0.39 有 CVE-2024-47874；0.115.3 起要求 starlette>=0.40
# starlette 本来是 fastapi 的传递依赖，这里显式钉住：CVE-2024-47874（multipart 表单
# 无上限占内存 → 拒绝服务）的修复版本是 0.40.0，写出来才看得见这条下限是有意的。
# Pinned explicitly although it's transitive: CVE-2024-47874 (unbounded
# multipart/form-data memory use → DoS) is fixed in 0.40.0, and spelling the floor
# out is the only way that floor stays visible.
starlette==0.46.2
uvicorn[standard]==0.30.6
sqlalchemy==2.0.34
pydantic==2.9.2
pydantic-settings==2.5.2
PyJWT==2.9.0
bcrypt==4.2.0
python-multipart==0.0.20    # CVE-2024-53981：畸形 multipart 边界触发过量日志与 CPU 占用，0.0.18 起修复
pandas==2.2.2
numpy==1.26.4
email-validator==2.2.0
psycopg2-binary==2.9.9
slowapi==0.1.9
google-auth==2.35.0
requests==2.32.5            # CVE-2024-47081：特制 URL 可致 .netrc 凭据泄漏，2.32.4 起修复
pywebpush==2.0.0
httpx==0.27.2
# 跨进程共享状态（REDIS_URL 配了才连；没配也要装，import 在配了时才发生）
redis==5.0.8
